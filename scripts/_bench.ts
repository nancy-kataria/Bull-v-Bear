// Shared constants for the benchmark scripts. Seed data and the pipeline
// benchmark both reference this dedicated user so benchmark rows stay isolated
// from real data and can be cleaned up in one delete.
export const BENCH_USER_EMAIL = "benchmark@bull-v-bear.local";
